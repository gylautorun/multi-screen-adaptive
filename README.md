# 前端可视化与多屏适配项目

## 📋 项目介绍

本项目是一个基于Vue 3和TypeScript的前端可视化系统，专注于提供强大的数据展示和交互能力，并支持多屏适配。项目整合了多种先进的可视化技术，适用于数据监控、大屏展示、指挥调度等各类场景需求。

## 🎯 项目特色

### 1. 多屏适配能力

-   支持多种分辨率显示器，包括1K到4K屏幕
-   内置响应式布局方案，确保内容在不同设备上合理展示
-   提供可配置的PostCSS转换方案，支持px转viewport单位（按需启用）
-   自定义视口适配策略，满足不同场景的显示需求
-   实现了 `useResize` 自定义Hook，用于自动计算和应用缩放比例

### 2. 可视化能力

-   集成ECharts和vue-echarts，提供丰富的图表类型和定制能力
-   集成Anime.js动效库，实现流畅的动画交互

### 3. 现代化架构设计

-   基于Vue 3 Composition API构建，代码组织更清晰
-   完整的TypeScript类型支持，提供良好的开发体验
-   模块化状态管理（Pinia），数据流可控
-   集成i18n国际化框架（已配置但需在组件中实际使用$t或useI18n）

### 4. 丰富的实用组件与功能

-   基于Element Plus构建UI组件库
-   自定义SVG图标系统，支持组件化使用
-   多种自定义指令（copy、throttle、debounce、auth、draggable、water-marker）
-   实用工具Hook：
    -   `useResize`：屏幕适配与缩放
    -   `useSeamlessScroll`：基于Anime.js的无缝滚动
    -   `useSortable`：基于Sortable.js的拖拽排序

## 🚀 快速开始

### 环境要求

-   Node.js >= 16.0.0
-   pnpm >= 8.0.0

### 安装依赖

```bash
# 安装项目依赖
pnpm install
```

### 开发环境启动

```bash
# 启动开发服务器
pnpm dev

# 构建生产版本
pnpm build

# 构建开发版本
pnpm build:dev

# 预览构建后的应用
pnpm preview
```

### 代码质量检查

```bash
# 类型检查
pnpm type-check

# ESLint检查
pnpm eslint
```

## 📦 部署指南

### 构建与部署

```bash
# 构建生产环境
pnpm build

# 构建产物位于dist目录
```

### 静态部署

1. 将 `dist` 目录下的所有文件部署到静态文件服务器（如Nginx、Apache等）
2. 配置服务器支持History模式路由（以Nginx为例）：

```nginx
server {
    listen 80;
    server_name example.com;
    root /path/to/dist;
    index index.html;
    # 支持History模式路由
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

### 环境配置

项目支持通过环境变量进行配置，主要配置文件包括：

-   `.env`：基础配置
-   `.env.development`：开发环境配置
-   `.env.production`：生产环境配置

## 🔧 项目结构

```
src/
├── assets/           # 静态资源
├── components/       # 公共组件
│   └── svg-icon.vue  # SVG图标组件
├── directives/       # 自定义指令（copy、throttle、debounce、auth、draggable、water-marker）
├── hooks/            # 自定义Hook（useResize、useSeamlessScroll、useSortable）
├── i18n/             # 国际化配置（已配置但需在组件中实际使用）
├── pages/            # 页面组件
│   └── big-screen/   # 大屏展示页面
├── router/           # 路由配置
├── stores/           # 状态管理（Pinia）
├── styles/           # 样式文件
├── types/            # TypeScript类型定义
├── utils/            # 工具函数
├── main.ts           # 应用入口
└── register.ts       # 组件注册
```

## 📱 多屏适配实现

### 核心适配方案

1. **SCSS变量系统**：在 `styles/screen.scss`中定义不同屏幕尺寸的变量
2. **响应式布局**：使用Flexbox和Grid布局系统实现自适应
3. **PostCSS转换**：提供 `postcss-px-to-viewport`配置，可按需启用
    - 支持配置视口宽度、转换精度等参数
    - 可通过类名前缀排除特定元素不进行转换
4. **屏幕监听Hook**：`use-resize` Hook监听屏幕尺寸变化，动态调整样式

### 适配配置说明

在 `vite-plugin/postcss-px-to-viewport-config.ts`中可配置视口转换参数：

-   设计稿宽度（默认750）
-   转换单位（默认vw）
-   转换精度（默认6位小数）
-   排除选择器等

## 🛠️ 核心技术栈

### 前端框架

-   Vue 3.4+
-   TypeScript 5.4+
-   Vite 5.2+

### 状态管理与路由

-   Pinia 2.1+
-   Vue Router 4.0+

### UI与可视化

-   Element Plus 2.7+
-   ECharts 5.5+
-   Anime.js 3.2+（用于动画效果）
-   Sortable.js 1.15+（用于拖拽排序）

### 工具库

-   Axios 1.7+（HTTP请求）
-   Dayjs 1.11+（日期处理）
-   Vueuse 10.10+（Vue组合式API工具集）
-   Lodash-es（工具函数）

### 其他功能模块

-   vue-i18n 9.13+（国际化支持）
-   自定义指令系统
-   SVG图标系统

## 🔗 相关文档

-   [Vue 3官方文档](https://v3.vuejs.org/)
-   [TypeScript官方文档](https://www.typescriptlang.org/)
-   [Vite官方文档](https://vitejs.dev/)
-   [ECharts官方文档](https://echarts.apache.org/zh/index.html)
-   [Element Plus文档](https://element-plus.org/)
-   [Pinia官方文档](https://pinia.vuejs.org/)
-   [Anime.js文档](https://animejs.com/)
-   [Sortable.js文档](https://sortablejs.github.io/Sortable/)
-   [VueUse文档](https://vueuse.org/)
